Repository for SoftImpute-ALS Python Implementation

* Note: Only data visualization code is ready at the moment

* It is required to have a local directory called 'data' holding the
  MovieLens-100k data set in order to use these modules. 

* Specifically, the file ./data/ml-100k/u.data must exist in the project root
  This can be found at https://grouplens.org/datasets/movielens/100k/ 


